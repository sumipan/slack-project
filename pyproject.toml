[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "slack-project"
version = "0.1.1"
requires-python = ">=3.11"
dependencies = ["ghdag"]

[project.optional-dependencies]
dev = ["pytest"]
